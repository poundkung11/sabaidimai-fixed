/**
 * Reference-only note.
 *
 * These route snippets were previously kept here as a scratch file, but the
 * active backend now lives in `backend/server.js`.
 *
 * If you need to add or modify app/admin APIs, update `server.js` directly.
 */

module.exports = {
  referenceOnly: true,
  activeServerFile: 'backend/server.js',
};
